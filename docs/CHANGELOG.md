# 更新紀錄

只記重要改動;逐個 commit 嘅細節睇 `git log`。

## 2026-09-26

### 新功能:開路線自動打開最近你嘅站

- 由搜尋開一條線,會用你嘅位置搵呢條線離你最近、上得車嘅站(尾站淨係落車,唔計),
  1 公里內就自動展開到站時間並捲過去;頂部顯示「📍 最近你:某某站 · 約 N 米」,撳返佢會再跳返去
- 太遠(例如喺第二區查線)只會提示最近嗰個站同距離,唔會亂跳;撳「打開」先開
- 路線地圖用藍點標你嘅位置;連續開幾條線會用返 2 分鐘內嘅定位,唔使次次等 GPS
- 你自己揀咗站、或者由收藏 / 返程帶站入嚟,都唔會被搶走;拒絕咗定位權限就唔會再問
- 設定(⚙️)可以閂,閂咗會變做一個「📍 搵最近我嘅站」掣自己撳

### 修正:地圖底圖出「API KEY REQUIRED」水印

- CARTO 由 2026 年 9 月起,冇 API key 嘅圖塊全部變成同一張「API KEY REQUIRED」水印圖(用 Actions 實測確認),
  所有地圖(路線、港鐵、路況、24/7、揀地點)都受影響
- 改用**地政總署**官方地圖 API:地形圖 + 繁體中文地名標籤,免 API key,香港街道 / 屋苑 / 港鐵站資料更齊;
  按條款喺地圖角落顯示地政總署 logo 同版權聲明。深色模式照樣反色
- 只喺香港範圍請求圖塊;縮到好細(z10 以下)用 z10 圖縮細,唔會一片空白
- `scripts/probe-tiles.mjs` + Probe live APIs workflow 嘅「tiles」選項:之後可以隨時喺 Actions 驗證底圖通唔通

### 移除:藍牙小屏推送(SKD-CLOCK)

- 設定入面「🖥️ 推送去藍牙小屏」同成個 Web Bluetooth 推送功能已移除(用家唔需要);
  相關 code 仍然喺 git 歷史,要用可以隨時搵返。門口顯示模式、收藏排序等照舊

## 2026-09-25

### 全面 audit + 修正(code / UI / 功能)

用 6 個角度(async 正確性、資料 / 網絡穩健、效能、UI 一致性 + 深色模式、無障礙、code 結構)審查成個 codebase,
每個發現都由兩個獨立 reviewer 驗證(真係會發生?修法安唔安全?),82 個入面 78 個確認後分 11 組平行修正,
每組再有 reviewer 覆核;最後再用 5 個角度(跨組整合、用家流程 regression、截圖視覺、無障礙 + 廣東話用字、效能)
覆核全部改動,29 個發現入面 24 個確認並修正。單元測試由 51 個增加到 521 個。

#### 效能

- 首屏 JS 271KB → 265KB(gzip 89 → 84KB),而且拆成兩份:app 本身 123KB(gzip 39KB)+ React 142KB(gzip 45KB);
  React 嗰份跨版本 hash 唔變,之後每次更新只需重新下載 39KB。規劃、附近、路線頁、顯示模式、設定等全部 lazy load,載入失敗自動重試一次
- 最大嘅實際慳位:唔再每次開 app 喺背景下載規劃圖(以前 674KB gzip)
- 規劃圖站 ID interning,2.26MB → 1.24MB(gzip 674 → 440KB),只喺打開規劃 / 附近城巴綠van 先載;SW 亦唔會預載佢
- 綠van 站序拆 16 份按需載入(以前開一條線要成份 144KB gzip)
- 路線地圖唔再每秒重繪;搜尋排序用共用 Intl.Collator + useDeferredValue;JSON 資料改 JSON.parse 字串
- 城巴站資料存 IndexedDB,再開同一條線唔使逐個站重新攞;門口顯示模式時鐘改每分鐘 render

#### 穩定性 / 正確性

- 每個 API 請求都有 timeout(iOS 15 都得);一個卡住嘅請求唔會再拖死成個收藏 / 附近 / 天氣
- 失敗唔再被當成功快取(天氣「正常」、綠van「冇班次」、規劃圖 / 車費整個 session 壞晒等)
- 輪詢唔再疊請求,斷網暫停、返網即刻更新;收藏每條線攞到就即刻顯示
- 網絡唔穩保留上次到站最多 5 分鐘並標示「📶 網絡唔穩」,錯誤訊息全部轉廣東話
- 九巴 ETA 分方向;輕鐵 751* 等特別線搵到車;嶼巴 10 組同號線唔再撞錯站;綠van / 嶼巴收藏帶 uid 唔會開錯區
- 附近每次開都重新定位(以前會沿用舊位置);斷網唔再話「附近冇車」
- 路線清單部分營辦商失敗唔會覆蓋完整快取;九巴站名過期都有舊資料後備
- Service worker:預載外殼同 JS/CSS、保留上一版快取,部署後舊分頁唔會爆 chunk 404;新版本提示「撳一下更新」;導航 3 秒 timeout 用快取
- 門口顯示模式:唔再顯示凍結咗嘅分鐘、兩隻手指掂唔會自己退出、#display 退出後唔會彈返入去
- 規劃:地址搜尋結果名唔再變「自訂位置」、第二個出門提醒會響、車費按上車站 ID 計

#### UI / 無障礙

- 修正深色模式卡片按鈕文字變黑色睇唔到;深色模式地圖、首次載入唔再閃白
- 新 color token(--accent-text / --accent-fill / --on-accent / --ok-text / --soon-text):粉紅字、白字粉紅底、到站分鐘全部過 WCAG AA,保留粉紅可愛風格
- 營辦商 / 港鐵綫 chip 深色模式睇得清,選中有 ✓;焦點框、44px 觸控、Esc 關面板、dialog 語意、reduced motion
- 有收藏時首頁改為細公仔一行問候,收藏排第一;頂欄裝飾唔再遮字

#### 新功能

- 🚶 **步行時間 +「趕唔趕到」**:收藏設步行時間,標示趕唔切 / 搭呢班 / 最遲幾點出門;附近按距離估
- 🚇 **港鐵站收藏上首頁**:下兩班車 + 月台 + 延誤提示;鐵路分頁記住上次揀嘅綫同站
- ⇄ **一撳返程** + 開路線自動捲去你個站
- 班次可信度標籤(預定 / 尾班車)、✨ 新版本提示
- 門口顯示模式橫屏 2×3 放 6 個站、設定入面自由排序收藏

## 2026-09-05

### 修正:搵唔到某啲九巴線(例如 38、42C)

九巴 38、42C 一直都喺清單入面,但同一個路線號城巴 / 綠van / 嶼巴都有,而排序用咗營辦商代碼
字母序(ctb < gmb < kmb),九巴被迫到後面 —— 搜尋「38」時九巴排第 6(前面 2 個城巴 + 3 個綠van),
喺手機上面已經跌出屏幕。已用 Actions 實測確認 KMB API 有齊兩條線同 ETA。

- **同號時九巴排第一**(kmb → 城巴 → 嶼巴 → 綠van → 輕鐵);38 同 42C 而家都排第 1
- **搜尋認得營辦商前綴**:「九巴38」「城巴1」「ctb 1」直接篩走其他營辦商;淨打「嶼巴」列晒佢嘅線
- **輸入正規化**:全形「４２Ｃ」、有空格「42 c」、尾綴「38號」「42C線」都搵到
- **站名有空格都夾到**(例:「圖書館,高士威道」)
- 結果上限由 80 提到 200
- **修正快取毒化**:以前如果九巴 fetch 失敗但其他營辦商成功,個殘缺清單會寫入快取賴足 7 日,
  期間搵唔到任何九巴線。而家殘缺清單唔會寫快取;讀到冇九巴嘅舊快取會自動當佢冇效重新攞
- 搵唔到嘅時候,如果係某間營辦商資料載入唔到,提示會講明白
- 新增 `Probe live APIs` 手動 workflow(開發沙盒封 *.gov.hk,要查真實 API 就喺 Actions 跑)

## 2026-09-03

### 工程 / 品質

- **工具鏈**:ESLint 10(typescript-eslint + react-hooks v7)、Prettier、Vitest(jsdom)。`npm run check` = lint + typecheck + test。
  44 個單元測試(時間、快取、推薦、附近排序、規劃索引、搜尋、備份、IndexedDB、返回鍵、輪詢)。
- **CI**:`ci.yml` 所有分支 push / PR 跑 lint + format check + typecheck + test + build;deploy 前都會先跑 lint + test。
- **靜態資料可以重生**:`scripts/bake-static.mjs` 由 hkbus 上游重焗 `src/data/*.json`(有縮細保護);
  `bake-static.yml` 每月 1 號自動跑 + commit。今次已用 9 月上游資料更新(路線 / 站序 / 車費 / 綠van / 嶼巴 / 輕鐵)。
- **Deploy workflow**:trigger 加 `main`;24/7 分店 bake 改每週 cache(3 小時一次嘅 cron 部署由 3 分鐘減到約 1 分鐘)。
- **ErrorBoundary**:render 出錯唔再白畫面,有「重新載入 / 清走快取」掣。
- **SW cache 自動版本**:build 時注入 git sha,唔使人手 bump;`cache.put` 包 `waitUntil`。
- **大快取搬去 IndexedDB**(`src/lib/kv.ts`):路線清單、九巴站點唔再靠 localStorage 5MB 上限;舊資料自動搬遷。
- **結構**:App.tsx 搜尋頁抽做 `SearchView`;`EtaList`(展示)/ `EtaPanel`(自輪詢)分開;收藏一個 loop 攞晒 ETA;
  `index.css` 拆做 `src/styles/` 20 個檔(`@import` 次序不變)。
- 型別清理:`@types/web-bluetooth`、`requestIdleCallback` / `wakeLock` 用原生型別。
- 刪走 `docs/HANDOFF-OPUS.md`(2026-07 交接文件,內容已過時)。

### 功能 / UX

- **返回鍵**:Android 返回鍵 / 瀏覽器上一頁 / 邊緣滑動逐層退返上一頁(揀地點 → 路線詳情 → 分頁 → 首頁),唔再一撳就閂 App;
  門口顯示模式鎖定層只彈提示。
- **搜尋支援目的地 / 站名**(有中文就搵站名,純英數就路線號)。
- Topbar 捲動自動收細;tabs sticky 位置改用實際量度(修正被 topbar 遮住);搜尋卡「往 / 由」分兩行;ETA skeleton;
  未揀主題時跟系統深色;`theme-color` 跟主題。
- 淺色模式輔助文字對比度由 2.9:1 提升到 4.6:1(WCAG AA)。
- 所有定時刷新喺背景分頁自動暫停(`usePolling`);同一 API 多處同時要只發一次(`memoAsync`);
  九巴站點記憶體快取;附近最近站排序記住。
- 備份檔補返「是日金句」同「附近預設營辦商」。
- README / footer 加私隱說明(地址搜尋後備會傳送去 komoot Photon)。

## 2026-07 及之前

見 `git log`:門口顯示模式(kiosk)、藍牙 e-ink 小屏推送、24/7 分店、TSM 路況、行程規劃、落車鬧鐘、集印卡等。
